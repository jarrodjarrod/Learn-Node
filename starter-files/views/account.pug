extends layout 

block content
  .inner 
    h2= title
    form.form(action="/account", method="post") 
      label Name 
      input(type="text",name="name", value=user.name)
      label Email 
      input(type="email",name="email", value=user.email)
      input.button(type="submit", value="Update details ➡️")